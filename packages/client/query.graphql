query getPost($id: ID!) {
  post(id: $id) {
    id
    title
    body
    updatedAt
    revisedAt
  }
}

query getPosts {
  posts {
    edges {
      node {
        id
        title
        body
        updatedAt
        revisedAt
      }
    }
  }
}
