fragment postDetails on Post {
  id
  title
  body
  publishedAt
  revisedAt
}

query getPost($id: ID!) {
  post(id: $id) {
    ...postDetails
  }
}

fragment postListItem on PostInList {
  id
  title
  publishedAt
  revisedAt
}

query getPosts {
  posts {
    edges {
      node {
        ...postListItem
      }
    }
  }
}
